import React from "react";
import {alignCenter} from 'react-icons-kit/fa/alignCenter'
import Button from "../Button";
export default (props) => {
  return <Button {...props}  icon={alignCenter} />;
};