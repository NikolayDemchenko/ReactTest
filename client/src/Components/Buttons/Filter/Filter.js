import {filter} from 'react-icons-kit/icomoon/filter'
import React from "react";
import Button from "../Button";
export default (props) => {
  return <Button {...props} size={"100%"} icon={filter} />;
};